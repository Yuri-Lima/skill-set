Evals for `explain-in-browser` grade the routing decision (browser vs
terminal), the teaser-only TUI rule, and an explicit “keep it here”
override. They do not require a live browser or generated HTML fixtures.
