Evals for `explain-in-browser` grade the routing decision (browser vs
terminal), the teaser-only TUI rule, an explicit “keep it here”
override, and listen-in-page (not `say`). They do not require a live
browser or generated HTML fixtures.
