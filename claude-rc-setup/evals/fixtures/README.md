# Test fixtures — claude-rc-setup

Canned `probe.sh` / `verify.sh` output. The agent should decide
stop-or-proceed from these, not from a live `systemctl`.

Ground truth is in `../evals.json` under `expected_output`.
