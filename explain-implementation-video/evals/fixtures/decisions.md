# Locked decisions (cutover)

- Partner day-to-day login stays `admin`.
- PHX-only login is a new `superadmin` secret after cutover.
- Old shared password is retired; it must fail on the partner form.
